import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
	mealItemdetails: {
		flexDirection: "row",
		alignItems: "center",
		padding: 8,
		justifyContent: "center",
	},
	mealItemdetailsitem: { marginHorizontal: 4, fontSize: 12 },
});
